git is a distributed version contral soft;
git is free version contral soft ;
git haves a index called stage ;
git branch filename git checkout -b filename ;
git is easy and important;
branch merge --on-ff;
bug is a gift;
env.com again;
